import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tyng.app',
  appName: 'TYNG',
  webDir: 'www',
  server: {
    androidScheme: 'https',
  },
};

export default config;
